using Gtk 4.0;
using Adw 1;

template $HolocttyInspectorWindow: Adw.ApplicationWindow {
  title: _("Holoctty: Terminal Inspector");
  icon-name: "com.sfire.holoctty";
  default-width: 1000;
  default-height: 600;

  styles [
    "inspector",
  ]

  content: Adw.ToolbarView {
    [top]
    Adw.HeaderBar {
      title-widget: Adw.WindowTitle {
        title: bind template.title;
      };
    }

    Gtk.Box {
      orientation: vertical;
      spacing: 0;
      hexpand: true;
      vexpand: true;

      $HolocttyInspectorWidget inspector_widget {
        notify::surface => $notify_inspector_surface();
        surface: bind template.surface;
      }
    }
  };
}
